# MB-X.01 · TE Coherence Module (demo)
Usage:
python batch_eval.py --in sample.csv --out results.csv --bases 2 3 4

Outputs: TruthΩ, Co+, Score+ per view + median. 
Scope: misura/validazione, non generazione identitaria.